---
description: List the open Standing TODOs from CLAUDE.md
---

Read `CLAUDE.md` (project root), find the "Standing TODOs" section, and list every
open (unchecked) TODO back to me as a clean, numbered list. For each, include the
one-line description and any linked reference file.

If `$ARGUMENTS` is non-empty, treat it as a filter and only show TODOs matching it.

If there are no open TODOs, say so plainly. Do not invent or infer TODOs that
aren't written in that section.
