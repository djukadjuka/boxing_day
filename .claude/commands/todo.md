---
description: Add a new Standing TODO to CLAUDE.md (from a short note)
---

Add a new TODO to the "Standing TODOs" section of `CLAUDE.md`, based on this note:

$ARGUMENTS

Turn the short note into one clear, self-contained TODO bullet:
- Use the recent conversation/work as context to flesh out what the note actually
  means (the file or system involved, what "done" looks like), so it still makes
  sense in a future session with no memory of today.
- Keep it to one or two lines, matching the format of the existing TODOs
  (`- [ ] **Title** — detail`). Link a relevant file/doc if there's an obvious one.
- Append it under the existing open TODOs; don't reorder or remove others.

If the note is too vague to write a meaningful TODO and there's no useful context to
draw on, ask me one quick clarifying question before writing it.

Show me the final TODO line you added.
